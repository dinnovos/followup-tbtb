# Copy this file to .env and set a real password before running
# `docker compose up`. Use the SAME value in api/FollowUp.Api/appsettings.Development.json
# (see appsettings.Example.json) -- these two files are not linked automatically.
MSSQL_SA_PASSWORD=ChangeMe_2026!
